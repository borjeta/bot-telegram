#action.yml que se ejecutará despues del build.yml y que levantará 
#un servidor web en el puerto 3000 que ejecutará el main.js
name: 'action'
description: 'Enviar mensaje a Telegram'
runs:
  using: 'node16'
  main: 'main.js'
#
# Path: .github\actions\main.js
  